<nav class="navbar navbar-default">

  <div class="container">
     <div class="navbar-header">
      <%= link_to image_tag(asset_path('long-logo.svg')), root_path, :class => 'navbar-brand'  %>
    </div>

    <ul class="nav navbar-nav navbar-right">new_wiki_path
      <li>
        <%=
        link_to_if(user_signed_in?, "Write a wiki",  new_wiki_path, :class => 'navbar-link gray' ) do
          link_to("Write a wiki",new_user_session_path, :class => 'navbar-link gray')
        end
        %>
      </li>
      <% if user_signed_in? %>

      <li>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link logout'  %>
      </li>
      <li>
        <a><%= "Hello, #{current_user.username}"  %></a>
      </li>
      <% else %>
      <li>
        <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link sign-up'  %>
      </li>
      <li>
        <%= link_to "Log in",  new_user_session_path, :class => 'navbar-link'  %>
      </li>
      <% end %>
    </ul>
  </div>
</nav>
